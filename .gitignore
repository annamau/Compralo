node_modules/
.env
.env.*
!.env.example
dist/
build/
.DS_Store
*.log
/target
*.db
*.db-shm
*.db-wal
# --- Node (extension/, money/, intelligence/) ---
# --- Rust (backend/) ---
backend/target
# --- SQLite (backend/) ---
*.sqlite
*.sqlite-shm
*.sqlite-wal
# --- Python (intelligence/, si P3 elige Python) ---
__pycache__/
.venv/
*.pyc
# --- Secrets ---
# --- OS ---
Thumbs.db
# deploy/ — real keys live here and never in git (deploy/.env.example is the template)
deploy/.env
# DATABASE_URL defaults to buy-agent.sqlite, which is a dev artifact.
