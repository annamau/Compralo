# What never needs to travel to the VM.
#
# deploy/.env is deliberately NOT excluded: it is the one secret the VM must receive,
# and the whole point of `push`. The three application .env files below ARE excluded —
# compose supplies those values from deploy/.env instead.
.git/
.github/
node_modules/
target/
dist/
build/
data/
*.log
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
.DS_Store
/.env
/.env.local
p4/.env
autobuy/packages/backend/.env
